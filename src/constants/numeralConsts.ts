const minPasswordLength = 5;
const minRating = 0;
const maxRating = 10;
const minPrice = 0;
const maxPrice = 100;
const closeModal = 0;
const recentlyaddedAmount = 3

export {
  minPasswordLength,
  closeModal,
  minRating,
  maxRating,
  minPrice,
  maxPrice,
  recentlyaddedAmount
}
