const phoneMask = '+###(##)###-##-##';
const dateMask = '##.##.####';
const cardMask = '####-####-####-####';

const successMessage = 'Success!';
const passwordErrorMessage = 'password is too short !';
const confirmPasswordErrorMessage = 'password is not appropriate to repeated password !';
const emailErrorMessage = 'wrong Email !';
const passwordChanged = 'Password has been changed!';
const nouserError = 'there is no such User !';
const userCreatedMessage = 'User has been created!';
const userExistsMessage = 'this User is already exists!';
const emptyFieldMessage = "field shouldn't be empty";
const loginFirst = 'Log in/ Register first !';
const imageError = 'wrong image path';
const priceError = 'wrong price';
const genreError = 'wrong genre';
const ratingError = 'wrong rating';
const phoneError = 'wrong phone number';
const dateError = 'wrong date';
const ageError = 'wrong age';
const cardError = 'wrong payment card data';
const invalidfields = 'fields are not valid';
const submitFailed = 'submit failed';

export { 
  passwordErrorMessage,
  successMessage,
  confirmPasswordErrorMessage,
  emailErrorMessage,
  passwordChanged,
  nouserError,
  userCreatedMessage,
  userExistsMessage,
  phoneMask,
  dateMask,
  emptyFieldMessage,
  loginFirst, 
  imageError,
  priceError,
  ratingError,
  invalidfields,
  phoneError,
  dateError,
  submitFailed,
  ageError,
  cardError,
  cardMask,
  genreError
};
